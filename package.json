{
  "name": "<TODO: your-sidekick-name>",     // Change this to something like "sidekick-acme"
  "version": "0.1.0",
  "devDependencies": {
    "@fixieai/sdk": "*",
    "@tsconfig/node18": "^2.0.1",
    "@types/lodash": "^4.14.198",
    "typescript": "5.1.3"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "yarn build && fixie-serve-bin --packagePath ./dist/index.js --port 8080",  // for npm change 'yarn' to 'npm run'
    "typecheck": "tsc -p tsconfig.json",
    "build": "yarn run typecheck",  // for npm change 'yarn' to 'npm'
    "prepack": "yarn build"         // for npm change 'yarn' to 'npm run'
  },
  "dependencies": {
    "@fixieai/fixie": "^1.0.13",
    "ai-jsx": "0.15.0"
  },
  "files": [
    "dist",
    ".env"
  ],
  "main": "dist/index.js"
}